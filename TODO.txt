- Clean up the generated/merged classes from the code paths
- Remove duplicates when handling list[Type] | list[Type2] | List[Type | Type2]
- Consider class naming to remove the Type
- Consider class naming to remove the plural type if usage doesn't indicate list
